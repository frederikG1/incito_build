/*
 * Snapping lives with the renderer, so a page printed as published can
 * line its elements up the same way a tile does — see `IncitoPage`.
 */
export { shifted, snap, targetsFrom, SNAP_WITHIN, type Guide, type SnapRect as Rect, type Snapped } from '@incitio/renderer';
